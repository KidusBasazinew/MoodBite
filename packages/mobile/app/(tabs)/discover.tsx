import { View, Text } from 'react-native';

export default function discover() {
   return (
      <View className="flex-1 justify-center items-center">
         <Text className="text-xl font-bold">Discover Screen</Text>
      </View>
   );
}
